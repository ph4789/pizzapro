import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: 56,
    borderRadius: 12,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#B83341', // cor fixa
    paddingHorizontal: 24,
  },
  title: {
    fontSize: 14,
    color: '#FFF',
    fontFamily: 'DMSans_400Regular',
  },
  load: {
    // customiza se quiser
  },
});
